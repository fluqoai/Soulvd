fix(content): move office address from Riyadh to Al Khobar (AR + EN)

User moved the company's office city. Updated every surface that
referenced الرياض / Riyadh so the public site, the contact section,
the footer, the hero chat preview, the case-study seed data, and
the generated .docx templates all stay in sync.

Files touched:
  messages/ar.json          الرياض -> الخبر
  messages/en.json          Riyadh -> Al Khobar
  src/components/home/HeroChat.tsx
                            both languages of the chat bubble
  scripts/seed-content.mjs  case study (AR + EN) and the
                            "Dar Ar-Riyadh Real Estate" client
                            name -> "Dar Al Khobar Real Estate"
  scripts/generate-templates.mjs
                            BRAND_ADDRESS in the .docx generator
  public/templates/*.docx   regenerated so the supplier block in
                            the invoice template shows the new
                            address

No new strings added, no DB migration needed. If the seed script
was already run, the DB rows still show Riyadh — run the seed
again or update them by hand if those case studies / clients
are visible on the live site.
